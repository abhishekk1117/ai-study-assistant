{
  "buildCommand": "npm --prefix client run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && npm --prefix client install && npm --prefix api install",
  "regions": ["sfo1"]
}
